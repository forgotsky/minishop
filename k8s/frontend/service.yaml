apiVersion: v1
kind: Service
metadata:
  name: web-shop-frontend-service
  labels:
    app: web-shop-frontend
spec:
  selector:
    app: web-shop-frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
